<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shortlink.mapper.LinkMapper">

    <resultMap id="BaseResultMap" type="com.shortlink.entity.LinkDO">
        <id column="id" property="id"/>
        <result column="short_code" property="shortCode"/>
        <result column="gid" property="gid"/>
        <result column="origin_url" property="originUrl"/>
        <result column="favicon_url" property="faviconUrl"/>
        <result column="title" property="title"/>
        <result column="expire_time" property="expireTime"/>
        <result column="status" property="status"/>
        <result column="del_flag" property="delFlag"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, short_code, gid, origin_url, favicon_url, title, expire_time, status, del_flag, create_time, update_time
    </sql>

    <delete id="deleteByUsername">
        DELETE FROM link_link WHERE gid IN (
            SELECT gid FROM link_group WHERE username = #{username}
        )
    </delete>

</mapper>
