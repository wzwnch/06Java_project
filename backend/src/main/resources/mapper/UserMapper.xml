<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shortlink.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.shortlink.entity.UserDO">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="phone" property="phone"/>
        <result column="mail" property="mail"/>
        <result column="real_phone" property="realPhone"/>
        <result column="real_mail" property="realMail"/>
        <result column="del_flag" property="delFlag"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, username, password, phone, mail, real_phone, real_mail, del_flag, create_time, update_time
    </sql>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM link_user
        WHERE username = #{username} AND del_flag = 0
    </select>

    <delete id="deleteByUsername">
        DELETE FROM link_user WHERE username = #{username}
    </delete>

</mapper>
